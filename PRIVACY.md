# 隐私说明

## API Key 处理方式

- **macOS：** API Key 仅在当前 App 运行期间保存在内存中，退出后清除；不读取或写入 macOS 钥匙串。
- **Windows：** API Key 使用 Windows Credential Manager 保存，不写入源码、日志或普通配置文件。

两个平台都不会把 API Key 写入翻译检查点或诊断报告。

## 电子书内容

用户主动选择 EPUB 后，App 在本机解析书籍、分割正文并重建译本。为了完成翻译，待翻译文本会发送给用户选择并配置的 Qwen 或 DeepSeek 云端服务。封面、书内图片和其他无需翻译的资源由本机复制到输出文件，不作为翻译文本上传。

云端服务对数据的处理受相应服务商条款和隐私政策约束。使用前请自行确认其政策是否符合您的需求。

## 本地数据

App 可在本机保存翻译进度，以便失败后继续；进度数据不包含 API Key。用户生成的译本只会写入用户选择的位置。

## 诊断信息

用户主动复制诊断信息时，App 会对已知密钥和本地路径进行脱敏。提交问题前仍请人工检查，切勿上传 API Key、私人 EPUB、受版权保护的书籍正文、用户名或本机路径。

## 遥测

当前 App 未集成广告、分析或遥测 SDK。
